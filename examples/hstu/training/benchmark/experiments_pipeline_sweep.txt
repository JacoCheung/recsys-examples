# HSTU Pipeline Schedule Sweep
# ============================================================================
# Used by junzhang/rework-mtms to A/B the new SchedulablePipeline against
# itself with different thread_map variants. All runs share gin opts —
# the variant is selected via the HSTU_THREAD_MAP_VARIANT env var
# (forwarded by slurm_job.sub).
#
# Common opts: cutlass attn, recompute LN, balanced shuffler, prefetch
# pipeline + DynamicEmb caching at ratio 0.1, zipf 1.05.
#
# Submit one variant at a time, e.g.:
#   bash .cursor/skills/run-benchmark/submit_remote.sh \
#       --container-image=<image> \
#       --pipeline-backend=new \
#       --thread-map-variant=default \
#       --exp-file=training/benchmark/experiments_pipeline_sweep.txt \
#       --wait-and-analyze --scp-dest=vnc:/home/recsys-example-dashboard/benchmark/
# ============================================================================

exp_pipeline_baseline,--balanced_shuffler --kernel_backend cutlass --recompute_layernorm --caching --evict lfu --ratio 0.1 --pipeline_type prefetch --value_dist zipf --value_dist_alpha 1.05
